// npm init -y en la consola para instalar npm en la carpeta
// luego se instala el archivo necesario con npm i "nombre de la libreria"

//const validator = require('validator')
//console.log(validator.isEmail('luis@gmail.com'))
//console.log(validator.isURL('luis.com'))

/*
const chalk = require('chalk')
function adivinanza(numero) {
    if (numero == 5) {
        return chalk.green('Adivinaste')
    } else {
        return chalk.red('Sigue intentanto')
    }
}
console.log(adivinanza(7))*/